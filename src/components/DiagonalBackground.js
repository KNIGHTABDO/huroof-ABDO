'use client';

import './DiagonalBackground.css';

/**
 * Diagonal X-split background matching the reference:
 * Orange triangles at top-left and bottom-right corners
 * Green triangles at top-right and bottom-left corners
 */
export default function DiagonalBackground() {
  return <div className="diagonal-bg" />;
}
